Important: v2.0.7 fixes a bug in earlier versions that could silently overwrite Recovered and Marked photo classifications (and clear Original Names) when using Parallel Library. v2.0.7 also adds support for very long file paths inside large Google Takeouts, proper handling of network-drive Library Drives, and clearer error messages when a ZIP file is unreadable.

Please install now — your photo metadata is at risk on older versions, and updates from v2.0.7 onwards will install automatically.
